declare
    function countdown
        if num > 5
            sub num 1
            read num
            call countdown
        endif
    endfunction

    function otherDialog
        dialog "Test test test"
        read test
    endfunction

    variable num 10
    variable test "Let's read this var"
enddeclare

call countdown

read num